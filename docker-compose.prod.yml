version: "3.9"
services:
  mongodb:
    image: mongo:6.0
    container_name: mongodb
    restart: always
    volumes:
      - mongo-data:/data/db
    ports:
      - "27017:27017"

  api-gateway:
    image: ${DOCKERHUB_USERNAME}/cafe-api-gateway:latest
    container_name: api-gateway
    ports:
      - "3000:3000"
    depends_on:
      - menu-service
      - order-service
      - payment-service
      - inventory-service
      - customer-service

  menu-service:
    image: ${DOCKERHUB_USERNAME}/cafe-menu-service:latest
    container_name: menu-service
    depends_on: [ mongodb ]

  order-service:
    image: ${DOCKERHUB_USERNAME}/cafe-order-service:latest
    container_name: order-service
    depends_on:
      - mongodb
      - menu-service
      - inventory-service
      - customer-service

  payment-service:
    image: ${DOCKERHUB_USERNAME}/cafe-payment-service:latest
    container_name: payment-service
    depends_on:
      - mongodb
      - order-service

  inventory-service:
    image: ${DOCKERHUB_USERNAME}/cafe-inventory-service:latest
    container_name: inventory-service
    depends_on: [ mongodb ]

  customer-service:
    image: ${DOCKERHUB_USERNAME}/cafe-customer-service:latest
    container_name: customer-service
    depends_on: [ mongodb ]

volumes:
  mongo-data:
